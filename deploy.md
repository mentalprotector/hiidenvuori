# Deploy Plan: Hiidenvuori (Production)

## 1. Cloud Infrastructure: Yandex Serverless Containers
To keep hosting costs at zero (or minimal), we will use Yandex Cloud Serverless Containers.

### Action Items for Team:
- [ ] Prepare Dockerfile optimized for Serverless (stateless).
- [ ] Set up Yandex Cloud Registry for storing images.
- [ ] Configure `yc` CLI on the build server.

### Instruction for Client (Access):
We need access to their Yandex Cloud console to set up the environment.
*   **Step 1:** Client creates a Yandex Cloud account.
*   **Step 2:** Client adds our Yandex ID as `resource-manager.clouds.member` and `editor` (or we do it via a shared federation).
*   *Note: Faster to get their login/pass or be added as an administrator to their Cloud.*

---

## 2. Domain & DNS
The client has already purchased the domain `hiidenvuori.ru` (and possibly the cyrillic version).

### Action Items:
- [ ] Bind the domain to Yandex Serverless Container via Yandex Certificate Manager (for SSL).
- [ ] Update DNS records (A/AAAA or CNAME) at the registrar to point to Yandex Cloud.

---

## 3. Lead Collection: FormSubmit
To handle contact forms without a backend, we use [FormSubmit](https://formsubmit.co/).

### Action Items:
- [ ] Update all `<form>` tags to point to `https://formsubmit.co/mail@hiidenvuori.ru`.
- [ ] Perform the first test submission to activate the email.
- [ ] Configure custom redirect (thank-you page) after submission.

---

## 4. Final Deployment Steps
1.  **Build:** Run `docker build` with optimized assets (WebP, minified JS/CSS).
2.  **Push:** Push image to Yandex Container Registry.
3.  **Deploy:** Create/Update Serverless Container via `yc sls container revision deploy`.
4.  **Verify:** Check SSL and all pages on the main domain.

---
🅰️ *Plan generated by AI ABOBA.*
